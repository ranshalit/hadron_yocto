# Default journald console forwarding for Hadron NGX012.
# Routes all journal output to the active serial console (ttyTHS1 by default).
# Managed at runtime by /usr/sbin/hadron-serial-switch.
[Journal]
ForwardToConsole=yes
TTYPath=/dev/ttyTHS1
MaxLevelConsole=info
